name: Pipeline Automatizado
on:
  push:
    branches:
      - main
jobs:
  CI-ContinuousIntegration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Make new a .env file
        uses: SpicyPizza/create-envfile@v2.0.3
        with:
          envkey_PORT: 5000
          envkey_POSTGRES_PASSWORD: 16052021
          envkey_POSTGRES_USER: "postgres"
          envkey_POSTGRES_DB: "bingo_database_tests"
          envkey_DATABASE_URL: "postgresql://postgres:16052021@postgres:5432/bingo_database_tests"
          directory: .
          file_name: .env.test
      - name: Run tests
        run: docker compose -f docker-compose-test.yml run backend npm run test